# 第五章 基于Unity的交互式仿真系统设计

## 5.1 系统设计目标

本系统面向折纸结构的交互式建模与仿真分析，依托 Unity 2022.3.62f1 引擎实现“折痕设计-模型存储-三维重建-折叠仿真-结果交互”一体化流程。结合当前工程源码，系统设计目标主要体现在以下几个方面：

1. 实现折痕图的可视化构建。用户可在统一网格参考平面上完成顶点添加、折痕连接、顶点移动、顶点删除和折痕删除等操作，形成面向折纸模型的二维几何描述。
2. 建立统一的数据表达方式。系统以 JSON 作为折纸模型的中间表示，统一管理顶点、面、折痕、材质和全局参数，使设计模式与仿真模式能够通过文件进行解耦。
3. 实现三维折纸模型的自动重建。系统依据折痕图数据自动生成面片 Mesh、折痕线可视化对象以及基于刚体与铰链的物理连接关系，完成从折痕图到三维折纸对象的映射。
4. 提供可交互的折叠控制能力。用户可通过滑条、键盘和场景相机控制折叠进度、观察角度与模型加载过程，从而提升仿真系统的可操作性与演示性。
5. 支持设计与仿真双模式切换。系统在场景层面划分折痕设计模式与折叠仿真模式，使模型构建与行为验证能够在同一平台中连续完成。
6. 为后续研究扩展预留接口。源码中保留了半边结构、面查找、三角剖分、折痕属性编辑等支撑模块，可为后续单折痕控制、复杂面片处理和拓扑分析提供基础。

## 5.2 系统整体架构

从当前工程实现看，系统整体可划分为四个层次：数据层、设计编辑层、仿真执行层和交互展示层。

```mermaid
flowchart LR
    A["用户交互"] --> B["折痕设计层\nCreasePatternEditor"]
    B --> C["模型数据层\nOrigamiData / JSON"]
    C --> D["仿真加载层\nOrigamiLoader"]
    D --> E["物理控制层\nOrigamiController"]
    E --> F["展示与交互层\nSlider / Camera / Mode UI"]
```

### 5.2.1 数据层

数据层由 `Assets/scripts/OrigamiData.cs` 定义，核心数据对象包括 `OrigamiVertex`、`OrigamiFace`、`OrigamiCrease`、`OrigamiConnection`、`OrigamiMaterial` 和 `OrigamiModel`。其中：

- `OrigamiVertex` 用于描述折纸顶点坐标；
- `OrigamiFace` 用于记录面片顶点索引、刚体属性和颜色；
- `OrigamiCrease` 用于记录折痕端点、折痕类型、静止角、角度边界、刚度和显示宽度；
- `OrigamiModel` 作为统一容器，承载完整折纸模型。

该数据层既支撑折痕设计阶段的数据保存，也支撑仿真阶段的模型加载，是整个系统的数据基础。

### 5.2.2 设计编辑层

设计编辑层以 `Assets/scripts/CreasePatternEditor/CreasePatternEditor.cs` 为核心，负责在 `CreasePattern` 对象下完成折痕图编辑。该层直接响应用户在场景中的点击操作，并在网格平面上生成顶点球体与折痕线段。当前已实现的主要功能包括：

- 基于网格吸附的顶点创建；
- 两点式折痕创建；
- 顶点拖拽移动与相邻折痕同步更新；
- 顶点、折痕删除及索引重排；
- 山折、谷折、边界折痕的类型区分；
- 折痕图保存为 JSON 文件；
- 从 JSON 文件反向加载折痕图。

在保存过程中，系统会调用 `OrigamiFaceGenerator` 自动推导闭合面片，因此设计层不仅负责几何输入，还承担了初步拓扑组织功能。

### 5.2.3 仿真执行层

仿真执行层以 `OrigamiLoader` 和 `OrigamiController` 为主。`OrigamiLoader` 负责读取 JSON、生成面片对象、创建刚体与碰撞体、绘制折痕线，并依据面片邻接关系创建 `HingeJoint`；`OrigamiController` 负责统一维护铰链集合，接收折叠进度输入并驱动所有折痕联动。

该层构成了系统从“几何模型”到“可运动折纸对象”的关键桥梁。

### 5.2.4 交互展示层

交互展示层由 `DesignModeManager`、`SimulationLoader`、`SliderController`、`CameraController` 和 `GridCameraLink` 共同组成，主要负责：

- 设计模式与仿真模式切换；
- 模型路径输入与重新加载；
- 滑条与折叠进度的双向映射；
- 场景相机移动、旋转、缩放与重置；
- 参考网格显示与相机矩阵同步。

从 `SampleScene` 的配置来看，场景主要包含 `CreasePattern`、`OrigamiSystem`、`Main Camera`、`CreasePatternUI`、`SimulationUI` 和参考网格对象，形成了较清晰的双模式结构。

## 5.3 关键技术实现

### 5.3.1 基于 JSON 的模型驱动机制

系统采用 JSON 作为设计端与仿真端之间的桥梁。设计模式下，编辑器将顶点、折痕和自动生成的面片写入 `Assets/Models` 目录；仿真模式下，`OrigamiLoader` 根据相对路径或绝对路径读取模型文件，并解析为 `OrigamiModel`。这一机制降低了场景对象之间的直接耦合，使模型复用、版本保存和后续扩展更加方便。

当前工程中的 `CustomPattern.json` 和 `Miura0.json` 已体现出该数据驱动思路，说明系统支持实验模型与自定义模型的共同管理。

### 5.3.2 基于半边思想的面片自动生成

折痕编辑器本身主要维护“点-边”结构，而仿真加载阶段需要显式的面片信息。因此系统在保存时引入 `OrigamiFaceGenerator`，通过构造双向半边、按顶点极角排序并执行 face-walk 遍历，从折痕网络中自动识别闭合面片。对于边界折痕围成的外部区域，系统还会进行过滤，避免将外侧区域误识别为有效折纸面。

这一实现使折痕图不必由用户手动逐面标注，而是可由系统依据拓扑关系自动补全，提升了建模效率。

### 5.3.3 基于 Mesh 的折纸面重建

在 `OrigamiLoader` 中，每个折纸面被重建为独立的 `GameObject`，并挂载 `MeshFilter`、`MeshRenderer`、`Rigidbody` 和 `MeshCollider`。系统根据面片顶点顺序生成三角形索引，再调用 `RecalculateNormals()` 和 `RecalculateBounds()` 完成网格更新。这样既能够满足渲染需求，也为后续的物理仿真提供刚体对象基础。

当前实现采用扇形三角化方式，即以面片第一个顶点为公共顶点构造三角形，因此实现简单、效率较高，适合当前实验场景中的规则或近似凸多边形面片。

### 5.3.4 基于 HingeJoint 的折叠仿真

系统并未直接使用布料或软体求解，而是采用“面片刚体 + 折痕铰链”的方式进行折叠仿真。`OrigamiLoader` 会先找出同时包含某条折痕两个端点的相邻面片，再根据折痕方向、面片顶点顺序以及山折/谷折类型决定铰链挂载在哪一侧面上，最终配置：

- `anchor` 作为铰链中心；
- `axis` 作为折叠旋转轴；
- `JointLimits` 作为最小和最大折叠角；
- `JointSpring` 作为刚度、阻尼和静止角控制。

这种实现方式与折纸结构“沿折痕旋转”的物理含义一致，能够较直观地表达折痕驱动关系。

### 5.3.5 基于统一折叠进度的联动控制

`OrigamiController` 维护场景中全部有效 `HingeJoint`，并将 `foldProgress` 作为统一折叠控制参数。当用户通过键盘或滑条改变折叠程度时，控制器会按照每个铰链的角度范围线性计算目标角度，并写回 `spring.targetPosition`。同时，控制器还会根据当前铰链状态反算总体折叠进度，实现 UI 与物理状态同步。

这一方案适合当前原型系统中“整体同步折叠”的实验需求，能够以较低复杂度完成联动演示。

### 5.3.6 参考网格与双面材质支持

系统通过 `GridCameraLink` 将相机矩阵传递给自定义网格着色器，实现与视角联动的参考网格显示，便于用户在设计模式下进行对齐和观察。同时，工程中还提供了 `DoubleSidedAdvanced.shader`，可用于纸张正反面区分的双面渲染。这说明系统在基础物理仿真之外，也考虑了几何参考和视觉表达的一致性。

## 5.4 交互设计与功能实现

### 5.4.1 设计模式交互

在折痕设计模式下，用户围绕“顶点-折痕”两个核心对象进行操作：

- 点击添加顶点按钮后，可在参考平面上添加顶点；
- 点击添加折痕按钮后，通过两次选点建立折痕；
- 选择移动模式后，可拖动顶点并实时更新关联折痕；
- 选择删除模式后，可删除顶点或折痕；
- 通过折痕类型 Toggle 选择山折、谷折或边界折痕；
- 通过保存按钮将当前折痕图写入 `Assets/Models`。

为了保证编辑稳定性，系统对顶点采用网格吸附和邻近点检测；在删除顶点后，还会自动重排折痕引用关系，避免索引失配。

### 5.4.2 仿真模式交互

在仿真模式下，交互以模型加载和折叠控制为主：

- `SimulationLoader` 提供路径输入与加载按钮，支持根据模型路径加载 JSON；
- `SliderController` 将滑条值转换为整体折叠进度；
- `OrigamiController` 同时支持键盘上下方向键调节折叠角度，A/D 键旋转整体折纸系统。

从场景配置看，`OrigamiSystem` 对象集中承载了 `OrigamiLoader`、`OrigamiController` 和 `SimulationLoader`，使仿真操作入口较为集中。

### 5.4.3 相机与观察交互

`CameraController` 为系统提供了完整的场景浏览能力：

- `W/S/A/D` 控制前后左右移动；
- `Q/E` 控制上下移动；
- 鼠标右键拖动控制观察旋转；
- 鼠标中键拖动控制平移；
- 鼠标滚轮控制缩放；
- 按钮可将相机恢复至默认观察位姿。

这类自由视角控制适合折纸模型在不同折叠阶段下的空间观察，也有助于后续进行局部结构分析。

### 5.4.4 双模式切换机制

`DesignModeManager` 通过启用或禁用 `CreasePatternEditor` 与 `OrigamiController`，并控制 `CreasePattern`、`OrigamiSystem`、`CreasePatternUI` 与 `SimulationUI` 的显示状态，实现设计模式和仿真模式切换。该机制使系统形成了较清晰的任务分工：

- 设计模式面向折痕图构建；
- 仿真模式面向三维折叠展示与验证。

这种双模式组织方式符合交互式仿真系统中“建模-验证”分阶段操作的需求。

## 5.5 仿真边界与适用范围

结合当前源码实现，系统已经具备原型级交互仿真平台的基本能力，但其适用范围仍存在一定边界。

### 5.5.1 适用范围

当前系统更适用于以下场景：

- 规则折痕图或中小规模折纸结构的教学演示；
- 折纸拓扑关系与折叠联动机制的可视化验证；
- 基于 JSON 模型的快速原型设计与交互展示；
- 山折、谷折、边界折痕等基本折痕类型的建模与联动观察。

### 5.5.2 当前边界

1. 当前仿真控制以“整体统一折叠进度”为核心，尚未实现每条折痕独立驱动，因此更适合联动折叠演示，不适合复杂工序级折叠过程模拟。
2. `OrigamiLoader` 在加载时主要依据面片共享折痕端点来重建邻接关系，`connections` 字段在当前主流程中仍属于预留数据，尚未成为独立约束源。
3. 面片三角化当前采用简单扇形划分，`Triangulator` 工具类尚未接入主加载流程，因此对于凹多边形或复杂面片，网格重建结果可能不够稳定。
4. 折叠仿真基于刚体和 `HingeJoint`，在折叠幅度较大、面片较多或碰撞关系复杂时，仍可能出现穿模、抖动或局部不稳定现象。
5. 材质与颜色字段已经在数据结构中定义，但当前运行时主要依赖 Inspector 中指定的默认材质，尚未完整实现按 JSON 材质参数自动构建渲染效果。
6. 折痕属性编辑、平折类型等扩展能力已在代码中预留，但当前场景配置尚未全部打通，说明系统仍处于实验性集成阶段。
7. 设计模式与仿真模式之间当前以文件存储为主要桥梁，保存后若需立即验证最新模型，仍需结合模型重新加载流程完成更新。

因此，本系统更适合作为折纸建模与交互仿真的研究型原型平台，而非面向高精度工程分析的终态产品。

## 5.6 本章小结

本章结合 Unity 工程源码，对交互式折纸仿真系统的设计目标、整体架构、关键技术、交互流程和适用边界进行了梳理。当前系统已经形成了以 JSON 数据驱动为核心、以折痕设计与折叠仿真双模式协同为特征的实现框架，并通过折痕图编辑、自动面生成、刚体铰链仿真和交互式视角控制，完成了从几何建模到动态展示的基本闭环。

总体来看，该系统已经能够支撑折纸结构的可视化设计与交互验证，为后续开展单折痕独立控制、复杂面片稳健三角化、碰撞稳定性优化以及更高精度的动力学建模提供了实现基础。
